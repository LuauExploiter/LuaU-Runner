## Packages
react-simple-code-editor | Lightweight code editor component
prismjs | Syntax highlighting for the code editor
date-fns | Formatting dates for history

## Notes
Luau API expects POST /api/run with { code: string }
Response contains { output: string, error?: string }
